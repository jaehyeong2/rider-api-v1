spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
#logging:
#  level:
#    org.hibernate.SQL: debug


hash: test1111test1111test1111ttdd11@#

app:
  filePath: test/test

retrofitUrl: http://localhost